apiVersion: v1
kind: Service
metadata:
  name: tech-challenge-fiapeats-service
spec:
  selector:
    app: tech-challenge-fiapeats
  ports:
    - protocol: TCP
      port: 8081
      targetPort: 8081
  type: ClusterIP