apiVersion: apps/v1
kind: Deployment
metadata:
  name: fiapeats-ms-pagamento-deployment
  labels:
    app: fiapeats-ms-pagamento
spec:
  replicas: 1
  selector:
    matchLabels:
      app: fiapeats-ms-pagamento
  template:
    metadata:
      labels:
        app: fiapeats-ms-pagamento
    spec:
      containers:
        - name: fiapeats-ms-pagamentocontainer
          image: 980921733871.dkr.ecr.us-east-1.amazonaws.com/fiapeats-ms-pagamento:latest
          ports:
            - containerPort: 8081
          resources:
            limits:
              memory: "512Mi"
              cpu: "500m"
            requests:
              memory: "256Mi"
              cpu: "250m"
          readinessProbe:
            httpGet:
              path: /fiapeats/actuator/health
              port: 8081
            initialDelaySeconds: 90
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /fiapeats/actuator/health
              port: 8081
            initialDelaySeconds: 105
            periodSeconds: 10

      restartPolicy: Always
