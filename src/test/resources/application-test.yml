server:
  servlet:
    context-path: /fiapeats

spring:
  application:
    name: fiapeats

management:
  endpoint:
    health:
      show-details: always
  endpoints:
    web:
      exposure:
        include: health

feign:
  mercadopago:
    autenticacao: https://api.mercadopago.com/oauth/token
    pedido: https://api.mercadopago.com
  fiapeats:
    mspedido: http://localhost:8080/fiapeats